本章节主要讲解pandas对各种数据的操作，如下所示
pandas.merge(DataFrame,DataFrame,on=key)
表示在key键上连接
pandas.merge(DataFrame,DataFrame,left_on=key1,right_on=key2)
pandas.merge(DataFrame,DataFrame,how='outer|inner")
这里关于如何连接就是和数据库类似，这里不详细讲了
如果想在多个键上连接，那么就使用如下方式
pandas.merge(DataFrame,DataFrame,on=list,how='outer|inner')
pandas.merge(DataFrame,DataFrame,on=key,suffixes=())可以给连接的key取别名
pandas.merge(DataFrame,DataFrame,left_on=key,right_index=True,how='')
这种方式就是把DataFrame对象的索引用作连接的键了
DataFrame.join(DataFrame,how='')将两个DataFrame按照行连接起来
DataFrame.join([DataFrame,DataFrame])将把多个DataFrame连接起来
numpy.concatenate([Series,Series],axis=int)将在某个轴上连接几个series连接起来
pandas.concat([Series,Series,Series])将多个series合并起来
pandas默认是在axis=0上工作的，如果axis=1则结果便会一个DataFrame
pandas.concat([Series,Series],axis=1,join='inner|outer')将连接求交集
pandas.concat([Series,Series],axis=1,join_axes=[list])指定索引
如果向在连接上创建层次化索引，使用keys即可
pandas.concat([Series],keys=list)
如果在axis=1上执行，就会变成列名
pandas的concat也可以适用于DataFrame上
pandas.concat([DataFrame,DataFrame],ignore_index=True)将不会使用现有索引，只使用新索引
DataFrame.combine_first(DataFrame)这里将会结合两个DataFrame来生成一个DataFrame

