vim记录的不是git的文件，而是修改，比如
如下两种模式：
第一次修改---》git add ---》第二次修改---》git commit 
第一次修改---》第二次修改---》git add ---》git commit
就不一样，第一次只是第一次修改版本（最终结果）
第二次就是第二次修改版本（最终结果）
如果想查看不同
运行 git diff HEAD -- filename
其实这里的diff命令就是用最新的filename与工作区比较
