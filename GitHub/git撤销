git撤销修改，这个问题以前工作老是弄不懂，现在
可以说明白了
先例上命令格式
git checkout -- filename
--很重要，表示对象是文件，而不是分支
两种情景模式：
一，你现在工作区修改了文件，还未添加到工作区，这时候直接使用
git checkout -- filename放弃修改
二，你先在工作区修改了文件，还添加到了缓存区，这时候要用到
git reset HEAD file将文件先放回到工作区，然后使用第一种情景模式

